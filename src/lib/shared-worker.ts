// console.log("persistence worker");

// export const channel = new BroadcastChannel("automerge-demo-peer-discovery");

// channel.addEventListener("message", (evt: any) => {
//   console.log("SHARED WORKER MESSAGE", evt.data);
//   // on every change for a document, store in IndexDB

//   // but...how do you not overwrite existing changes?

//   // should it just behave like another peer, the only difference being that whenever it gets changes, it uploads to DB?
// });
